box: wercker/ruby
build:
  steps:
    - bundle-install
    - script:
        name: install packer
        code: |
          wget https://dl.bintray.com/mitchellh/packer/0.5.0_linux_amd64.zip
          unzip 0.5.0_linux_amd64.zip
          ./packer version

#    - script:
#        name: build image
#        code: ./packer build template.json

